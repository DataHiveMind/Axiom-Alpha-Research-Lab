tasks:
  Extract_Central_Bank_Data:
    shell: python -m robocorp.tasks run tasks.py -t ingest_macro_releases

environmentConfigs:
  - environment_windows_amd64_freeze.yaml
  - environment_linux_amd64_freeze.yaml
  - environment_darwin_amd64_freeze.yaml
  - conda.yaml

artifactsDir: output